#include <iostream>
#include <stdlib.h>
#include "numInt.h"

using namespace std;

int main(){
    class numInt lista;
    lista[5];
    lista.ins(1);
    lista.ins(2);
    lista.ins(3);
    lista.ins(4);
    lista.ins(5);
    lista.ins(6);
    lista.rem();
    lista.rem();
    lista.rem();
    lista.rem();
    lista.rem();
    lista.rem();
    return 0;
}
